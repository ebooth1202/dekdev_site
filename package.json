{
  "name": "dekdev-portfolio",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/build backend/"
  },
  "engines": {
    "node": "18.x"
  }
}
