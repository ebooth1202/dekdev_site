{
  "name": "dekdev-portfolio",
  "version": "1.0.0", 
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/build backend/",
    "start": "cd backend && python main.py"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
